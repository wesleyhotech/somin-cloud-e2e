# Copy to `.env` for local runs (optional). Do not commit real credentials.
# Playwright and Node load env from the shell; use `dotenv` or export manually if needed.

# Target site (default: https://cloud.somin.ai — see playwright.config.ts)
BASE_URL=

# Real account email for TC-N07 (wrong password). Leave empty to skip that assertion path.
E2E_VALID_EMAIL=

# Reserved for future positive-path / session tests (not required for current suite)
E2E_VALID_PASSWORD=
